
apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: osp-director-operator-webhook-service
spec:
  ports:
    - port: 443
      targetPort: 9443
  selector:
    control-plane: controller-manager
